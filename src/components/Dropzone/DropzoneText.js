import React from 'react'

import styled from 'styled-components'

const DropzoneText = ({ text }) => <Text>{text}</Text>

const Text = styled.p`
    font-family: Arial, Helvetica, sans-serif;
    font-size: 24px;
    font-weight: bold;
`

export default DropzoneText